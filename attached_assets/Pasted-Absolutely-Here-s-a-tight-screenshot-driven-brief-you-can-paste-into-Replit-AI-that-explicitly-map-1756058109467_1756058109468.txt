Absolutely. Here’s a tight, screenshot-driven brief you can paste into Replit AI that explicitly maps each issue in your screenshots to a fix and a measurable “done” state. It’s concise, but nothing is left ambiguous.

⸻

COPY/PASTE FOR REPLIT AI — FIX THE THREE REGRESSIONS FROM USER SCREENSHOTS

Do not change gameplay or rarity math. UI/UX only.

Context: What’s broken in the screenshots
	1.	Stats at the top are huge “cards.” They eat vertical space and push the grid down.
	2.	Grid is cropped to the left on mobile. Only the left columns are visible.
	3.	Player name bar shows as a solid black strip with no text after a guess.

⸻

A) Replace bulky stat cards with a compact strip

Problem (SS #1): Each metric is its own large panel.
Fix: One compact stats strip (no cards) that wraps to two rows on small screens.

Implementation

<div className="statsStrip" role="region" aria-label="Session stats">
  <div className="stat"><span className="val">{correct}</span><span className="lbl">Correct</span></div>
  <div className="stat"><span className="val">{incorrect}</span><span className="lbl">Incorrect</span></div>
  <div className="stat"><span className="val">{guessesLeft}</span><span className="lbl">Guesses Left</span></div>
  <div className="stat"><span className="val">{totalRarity}</span><span className="lbl">Total Rarity</span></div>
  <div className="stat"><span className="val">{avgRarity}</span><span className="lbl">Avg Rarity</span></div>
  <div className="stat"><span className="val">{bestRarity}</span><span className="lbl">Best</span></div>
  <div className="stat"><span className="val">{worstRarity}</span><span className="lbl">Worst</span></div>
</div>

.statsStrip{
  display:flex; flex-wrap:wrap; align-items:baseline; gap: clamp(8px,1.6vmin,14px);
  padding: clamp(8px,1.6vmin,12px) 0;
}
.statsStrip .stat{ display:flex; gap:6px; align-items:baseline; }
.statsStrip .val{ font-weight:800; font-size:clamp(14px,3.2vmin,20px); line-height:1; }
.statsStrip .lbl{ font-size:clamp(11px,1.8vmin,13px); opacity:.7; text-transform:uppercase; letter-spacing:.04em; }

Acceptance
	•	On iPhone width, strip is at most two lines high (no big cards).
	•	Grid is visible immediately under the strip.

⸻

B) Fix grid cropping (left side only visible)

Problem (SS #1 & #2): Grid container has a fixed width or wrong overflow; right columns are cut off.
Fix: Grid must use full available width, center itself, and always show 3 equal columns. On mobile, the main layout must be single-column (sidebar below grid).

Implementation

.main{
  display:grid;
  grid-template-columns: 1fr minmax(0, 380px);
  gap: clamp(12px,2vmin,20px);
}
@media (max-width: 900px){
  .main{ grid-template-columns: 1fr; }
}

.gridWrap{ width:100%; max-width:1100px; margin-inline:auto; overflow-x:hidden; }
.grid{
  display:grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: clamp(10px,1.8vmin,16px);
}
.grid .tile{ aspect-ratio: 1 / 1; min-width:0; position:relative; }

	•	Remove any fixed widths on grid parents (e.g., width: 900px, max-content).
	•	Ensure no parent is clipping the grid with overflow-x:hidden while also forcing inner width overflow. The .gridWrap above should be the only horizontal clip.

Acceptance
	•	At 320/375/414/768/1024/1280/1440px widths, the entire 3×3 grid is visible without horizontal scroll.
	•	On ≤900px, the sidebar stacks below the grid; no side-by-side squeeze.

⸻

C) Name bar shows as a black block without text

Problem (SS #3): The NameBar background renders but the text is invisible—likely due to backdrop-filter, z-index, or color token not resolving on iOS Safari.
Fix: Use a solid translucent background (no blur), force white text, correct z-index, and apply the previously agreed name-fitting rules.

Implementation

.nameBar{
  position:absolute;
  left: clamp(8px,2vmin,12px); right: clamp(8px,2vmin,12px);
  bottom: clamp(8px,1.6vmin,12px);
  padding: clamp(6px,1vmin,10px) clamp(10px,1.4vmin,12px);
  border-radius: clamp(10px,1.3vmin,14px);
  background: rgba(0,0,0,.45); /* solid translucent – no backdrop-filter */
  color: #fff;                  /* force readable text */
  z-index: 2;
  display:-webkit-box; -webkit-line-clamp:2; -webkit-box-orient:vertical;
  overflow:hidden; word-break:break-word; hyphens:auto;
  font-weight:800; font-size:clamp(12px,2.2vmin,18px); line-height:1.1;
}
.tile .rarityChip{ position:absolute; top:8px; right:8px; z-index:3; }

Name fitting (unchanged logic, ensure it runs):
	•	Try full name (up to 2 lines).
	•	If overflow, render F. Last[ Suffix] on one line with CSS ellipsis.
	•	If still too long, the last name truncates with ….
	•	Always set title and aria-label to the full name.

Acceptance
	•	After a guess, the name is clearly visible (white).
	•	Long names appear as F. Last… with tooltip showing the full name.
	•	The rarity chip never overlaps the name bar.

⸻

D) Rarity rank sentence display (confirm)

Problem (SS #4): The sentence should read 1 = rarest, N = most common.
Fix (display-only):

const displayRank = count - originalRank + 1; // if originalRank was 1 = most common
text = `Ranked ${displayRank} out of ${count} eligible players`;
hint = `1 = rarest · ${count} = most common`;

Acceptance
	•	For a very common star (e.g., all-timer on a popular team), rank is near N of N.
	•	For an obscure pick, rank approaches 1 of N.

⸻

Final QA (must match screenshots’ problem areas)
	1.	Stats strip: compact, at most two lines on phone; no big cards.
	2.	Grid: full 3×3 visible on mobile; no horizontal scroll; squares preserved.
	3.	Name bar: text visible (white), not just a black strip; long names fall back to F. Last…; chip never collides.
	4.	Modal rarity line: shows Ranked X out of Y with 1 = rarest hint.
	5.	Layout: on ≤900px, grid above, sidebar below; nothing is cut off on the right.

Please implement exactly as specified, touching only the UI.